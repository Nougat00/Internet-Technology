var obj = {
  className: "first bordered",
};

function addClassName(obj, word) {
  if (!obj.className.includes(word)) {
    obj.className = obj.className.concat(" " + word);
  }
}

addClassName(obj, "visible");
console.log(obj.className);
addClassName(obj, "visible");
addClassName(obj, "test");
addClassName(obj, "test");
console.log(obj.className);
